# Production-like preview: builds the site and serves it on http://127.0.0.1:8080
# For live-reload local development use `bun run dev` instead.
services:
  web:
    build: .
    image: app_store_srly_io:latest
    ports:
      - "8080:80"
